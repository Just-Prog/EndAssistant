import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function GachaRecordTabLayout() {
  return (
    <SafeAreaView>
      <View>
        <Text>EndAssistant Gacha Export Tool - Under Constructing</Text>
      </View>
    </SafeAreaView>
  );
}
