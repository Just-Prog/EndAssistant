import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function HomeTabLayout() {
  return (
    <SafeAreaView>
      <View>
        <Text>EndAssistant SKLand News & Monitor - Under Constructing</Text>
      </View>
    </SafeAreaView>
  );
}
