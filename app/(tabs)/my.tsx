import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function AccountTabLayout() {
  return (
    <SafeAreaView>
      <View>
        <Text>EndAssistant HG Account Manage Tool - Under Constructing</Text>
      </View>
    </SafeAreaView>
  );
}
